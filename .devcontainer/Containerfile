# Dev container for baz — mirrors scripts/toolbox-setup.sh (that script is the
# source of truth for the package list; keep them in sync).
FROM registry.fedoraproject.org/fedora:42

RUN dnf install -y \
      gcc gcc-c++ make git pkgconf-pkg-config \
      webkit2gtk4.1-devel gtk3-devel dbus-devel openssl-devel \
      librsvg2-devel libappindicator-gtk3-devel \
      alsa-lib-devel flac \
      nodejs npm \
    && dnf clean all

# Rust via rustup (pinned by rust-toolchain.toml once the workspace exists)
RUN useradd -m dev
USER dev
RUN curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs \
      | sh -s -- -y --default-toolchain stable --profile default
ENV PATH="/home/dev/.cargo/bin:${PATH}"
